<!DOCTYPE html>
<html>
<head>
  <title>Soma09</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="skin-white page-body <%= content_for(:login_page) %>">
<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">소마공구</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav navbar-right">
      <li><a href="#">Notification</a></li>
      <li class="notifications dropdown"> 
        <a href="#" class="dropdown-toggle notification" data-toggle="dropdown" data-hover="dropdown" data-close-others="true"> 
          <i class="entypo-bell"></i>
          <span id="new_noti_number" class="badge badge-secondary">
            <%= current_user.notifications.select{|notification| notification.read == false }.size %>
          </span> 
        </a> 
        <ul class="dropdown-menu">  
          <% current_user.notifications.last(8).reverse.each do |notification| %>
            <li>
            <ul class="dropdown-menu-list scroller" tabindex="5002" style="overflow: hidden; outline: none;"> 
              <li> 
                <a id="<%= notification.id %>" href="#<%= notification.post.id %>" class="scroll" data-read="<%= notification.read %>"> 
                  <span class="image pull-right"> 
                    <img src="<%= notification.send_user.image %>" alt="" class="img-circle" style ="width: 40px; height: 40px;"> 
                  </span> 
                  <% if notification.read %>
                    <span> 
                  <% else %>
                    <span class="no-read-notification"> 
                  <% end %>
                    <%= notification.send_user.name %>
                  </span> 
                  <span class="line desc small">
                    <%= notification.message %>
                  </span> 
                </a> 
              </li>   
            </ul> 
          </li> 
          <% end %>
          <li class="external"> 
            <a href="#">
            </a> 
          </li>
        </ul> 
      </li>

      <li>
        <%= link_to destroy_user_session_path , method: :delete do %>
          <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>

          로그아웃
        <% end %>
      </li>
    </ul>
  </div>
  <!-- /.navbar-collapse -->
</nav>
<%= render partial: "layouts/flash_messages", flash: flash %>
<%= yield %>
</body>
</html>
